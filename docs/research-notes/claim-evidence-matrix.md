# Claim-Evidence Matrix

Each row is a paper-level claim with evidence path, safe/unsafe wording, and limitation.

For further detail on any claim, see `docs/provenance-map.md`.

---

| Claim | Status | Evidence path | Test/report path | Safe wording | Unsafe wording to avoid | Limitation |
|-------|--------|--------------|-----------------|--------------|------------------------|------------|
| Stele is a proof checker | Stable | `stele/kernel.py` | `tests/test_kernel_valid.py`, `tests/test_kernel_invalid.py` | "Stele is a proof checker for natural-deduction reasoning" | "theorem prover," "proof assistant," "AI verifier" | Propositional only |
| Rule-schema-based verification | Stable | `stele/logic.py` (`RuleSchema`), `stele/kernel.py` | `tests/test_logic.py`, `tests/test_kernel_valid.py` | "The kernel verifies each step by syntactic pattern matching against rule schemas" | "semantic reasoning," "automated" | Only pattern-matches; no proof search |
| Multi-logic rule sets | Stable | `stele/logic.py` (5 logic objects) | `tests/test_logic.py`, `tests/test_kernel_valid.py` | "Five built-in logic objects; adding a logic means adding data, not modifying the kernel" | "supports any logic," "extensible framework" | Only the 5 built-in logics are provided |
| Classical vs intuitionistic distinction | Stable | `stele/logic.py`, `examples/dne.stele` | `RESULTS.md §상대성`, `tests/test_classical_principles.py` | "The same formula may be derivable under classical logic but not intuitionistic" | "proves relativism," "demonstrates logical pluralism" | Propositional rules only |
| Zero runtime dependencies | Stable | `stele/__init__.py`, `stele/kernel.py` | `tests/test_docs_and_deps.py` | "The trusted kernel uses only the Python 3.10+ standard library" | "lightweight" (vague) | pytest is a test-only dependency |
| Structural diagnostics | Untrusted / Stable codes | `stele/diagnostics.py` | `tests/test_diagnostics.py` | "Structural diagnostics (untrusted) provide 9 stable error codes" | "verified diagnostics," "trusted analysis" | Advisory only; not kernel-trusted |
| Dependency graph | Stable | `stele/proofgraph.py` | `tests/test_proofgraph.py` (if present) | "The proof dependency graph records step dependencies; output as Graphviz DOT" | "formal dependency verification" | Graph is structural; does not re-verify the proof |
| K3/LP matrix semantics | Demo | `stele/matrix.py` | `tests/test_matrix.py`, `test_k3_imp_table_matches_manifesto` | "K3 and LP matrix diagnostics demonstrate that LEM is not a K3 tautology" | "K3 proof system," "paraconsistent proof assistant" | Diagnostic tool; not a full proof system |
| Rule soundness checker | Demo | `stele/matrix.py` `rule_soundness()` | `tests/test_rule_soundness.py` | "Checks whether each rule preserves designated values in a given matrix" | "proves soundness" | Per-matrix diagnostic; not a soundness proof |
| Kripke countermodel search | Experimental | `stele/kripke.py` | `tests/test_kripke.py`, `tests/test_kripke_integration.py` | "Bounded finite-frame Kripke search finds countermodels for IPL formulas (up to N worlds)" | "complete," "decides IPL validity" | Bounded; no countermodel ≠ valid |
| Proof-term core (Curry–Howard) | Experimental | `stele/core/terms.py`, `stele/elaborate.py` | `tests/test_elaboration.py`, `tests/test_proof_term_typing.py` | "An experimental Curry–Howard proof-term core for IPL" | "full dependent types," "complete Curry–Howard" | IPL only; classical excluded |
| Bidirectional type checking | Experimental | `stele/core/typing.py` | `tests/test_proof_term_typing.py` | "Bidirectional type checking (infer/check modes) for STLC proof terms" | "full type inference," "polymorphic" | Simply-typed; no polymorphism |
| FOL proof-term fragment | Experimental | `stele/core/fol.py` | `tests/test_fol.py` | "An experimental FOL proof-term fragment (∀, ∃) at the proof-term level" | "first-order proof checker," "FOL support" | No surface language support; no equality |
| Classical negative translation | Experimental | `stele/core/classical_experimental.py` | `tests/test_classical_experimental.py` | "Gödel–Gentzen translation at the formula level (experimental)" | "classical proof terms," "λμ-calculus" | Formula-level only; no proof-term generation |
| Proof certificates (JSON) | Experimental | `stele/certificate.py` | `tests/test_minicheck.py` | "A versioned JSON certificate is emitted after kernel verification" | "formally certified," "independent verification" | JSON encoding only |
| Minicheck re-verifier | Experimental | `stele/minicheck.py` | `tests/test_minicheck.py` (tamper-detection) | "An independent software check at the code level; does not import the kernel" | "formally independent," "language-level isolation" | Same Python process; not process/language-isolated |
| Proof-state hints | Untrusted | `stele/proofstate.py` | `tests/test_proofstate.py` | "Advisory rule hints (untrusted, `trusted=False` always)" | "proof search," "trusted hints," "suggested proofs" | Structural pattern matching only |
| ML corpus discipline | Optional | `stele_ml/`, `bench/` | `tests/test_ml_corpus_discipline.py` | "A synthetic benchmark corpus with data-discipline policies and 3-way splits" | "real-world corpus," "human-annotated" | Synthetic, template-based |
| ML baseline metrics | Optional | `stele_ml/reports/baseline_report.json` | `tests/test_ml_corpus_discipline.py` | "Validity accuracy 0.85, exact-match 0.60 on a 40-record synthetic corpus (n=40)" | "85% accuracy," "state-of-the-art" | Small synthetic corpus; not generalizable |
| Lean 4 error bridge | Optional | `stele_lean/` | Tests in `stele_lean/` | "An optional Lean 4 error-output parser that maps type-error patterns to Stele codes" | "Lean integration," "Lean-compatible" | Error-message parsing only; no formal correspondence |
| Browser-local deployment | Stable | `site/studio.html`, `stele/browser.py` | `tests/test_pyodide_site.py` | "The full checker runs in-browser via Pyodide/WASM with no backend server" | "cloud-based," "serverless" (implies managed cloud) | CDN dependency; ~8 MB first-load |
| Single-file HTML | Experimental | `tools/build_single_html.py` (generated; not committed) | — | "A single-file HTML build bundles the Stele source with CDN Pyodide (generated artifact)" | "fully offline," "zero download" | CDN dependency for Pyodide runtime |
| Foundations / Yurihak research program | Motivation | `site/foundations.html`, `docs/foundations-program.md` | `tests/test_foundations.py` | "Stele's multi-logic architecture is motivated by the Yurihak research program" | "Stele implements Yurihak," "proves relativism" | Yurihak is not yet a formal Stele logic |
| Elaboration soundness (metatheory) | Experimental | `stele/elaborate.py` `crosscheck_theorem` | `tests/test_elaboration.py` | "Supported by regression tests and a proof sketch (not machine-checked)" | "formally verified," "proven" | Not machine-checked |
| Subject reduction / type preservation | Experimental | `stele/core/typing.py`, `stele/core/reduce.py` | `tests/test_reduce.py`, property-based tests (optional) | "Supported by a proof sketch and regression tests" | "proven," "verified" | Not machine-checked; fuel-bounded normalization |
| Kernel import invariant (⊢ ≠ matrix) | Stable | `stele/kernel.py`, `stele/matrix.py` | `tests/test_regression_invariants.py` | "Enforced by import isolation tests on every CI run" | "formally separated" | Python import check; not language-level |
